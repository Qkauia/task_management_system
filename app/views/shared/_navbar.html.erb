<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to t('navbar.home'), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to t('navbar.admin_interface'), admin_users_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t('tasks.link.english'), url_for(locale: 'en'), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t('tasks.link.chinese'), url_for(locale: 'zh-TW'), class: "nav-link" %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if current_user %>
          <li class="nav-item">
            <%= link_to t('navbar.logout'), logout_path, class: "nav-link", data: { turbo_method: "delete", turbo_confirm: t('users.confirm.logout') } %>
          </li>
          <li class="nav-item">
            <%= link_to t('users.headers.update_password'), edit_user_path(current_user), class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('navbar.login'), login_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('navbar.registration'), signup_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
