<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to t('navbar.home'), root_path, class: "navbar-brand" %>
    <%= link_to t('navbar.admin_interface'), admin_users_path, class: "navbar-brand" %>
    <%= link_to t('tasks.link.english'), url_for(locale: 'en'), class: "navbar-brand" %>
    <%= link_to t('tasks.link.chinese'), url_for(locale: 'zh-TW'), class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item">
            <%= link_to t('navbar.logout'), logout_path, data: { turbo_method: "delete", turbo_confirm: t('users.confirm.logout') } %>
          </li>
          <li class="nav-item">
            <%= link_to t('users.headers.update_password'), edit_user_path(current_user) %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('navbar.login'), login_path %>
          </li>
          <li class="nav-item">
            <%= link_to t('navbar.registration'), signup_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>